const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const root = path.resolve(__dirname, '..');
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8');
const manager = read('src/features/unified-agent/manager.ts');
const store = read('src/features/unified-agent/runtimeStore.ts');
const registry = read('src/features/unified-agent/toolRegistry.ts');
const protocol = read('src/features/unified-agent/protocol.ts');
const workspace = read('src/screens/WorkspaceScreen.tsx');
const nativeClient = read('src/features/unified-agent/nativeClient.ts');
const rust = read('../src-tauri/src/unified_agent_native.rs');
assert.match(manager, /const queues = new Map<string, QueueItem\[\]>/);
assert.match(manager, /acceptedRequestIds/);
assert.match(manager, /clientRequestId/);
assert.match(manager, /latestAccessibility/);
assert.match(store, /interrupted/);
assert.match(store, /localStorage/);
assert.match(registry, /Existing files must be read before editing/);
assert.match(registry, /composeSelectedPatch/);
assert.match(registry, /computer\.inspect/);
assert.match(registry, /models\.compare/);
assert.match(registry, /privacy\.classify/);
assert.match(registry, /computer\.activate/);
assert.match(registry, /current approved screenshot is required/i);
assert.doesNotMatch(registry, /git add -A/);
assert.match(protocol, /Accessibility/);
assert.match(workspace, /setPrompt\(''\)/);
assert.match(workspace, /UnifiedAgentRunCard/);
assert.match(workspace, /privacyAssessment/);
assert.match(nativeClient, /computer_inspect_accessibility/);
assert.match(nativeClient, /expectedRole/);
assert.match(rust, /AXPress/);
assert.match(rust, /Accessibility element identity changed after approval/);
assert.match(rust, /ALLOWED_APPS/);
console.log('CHRIS_STUDIO_V2_4_UNIFIED_AGENT_CONTRACT_PASSED');
